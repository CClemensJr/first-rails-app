<h1>Articles Tagged With <%= @tag.name %></h1><br>

<ul>
  <% @tag.articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul><br>

<%= link_to "<< Return to the tags list", tags_path %>
